// Modules are the building blocks of NestJS. They organize controllers, services and providers.
// Module command:- nest g module tasks
import { Module } from '@nestjs/common';

@Module({})
export class TasksModule {}
